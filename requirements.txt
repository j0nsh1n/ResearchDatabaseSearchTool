# Core dependencies
biopython>=1.84
requests>=2.32.0
pandas>=2.2.0
numpy>=2.0.0

# NLP and embeddings
sentence-transformers>=3.0.0
scikit-learn>=1.5.0

# Fast vector search (new in v2.3.0)
faiss-cpu>=1.8.0

# Density clustering (HDBSCAN "Density" method) reduces embeddings with UMAP
# first. Optional but strongly recommended: without it, HDBSCAN falls back to a
# PCA reduction that produces far more "outliers". Pulls in numba/llvmlite.
umap-learn>=0.5.6

# Web interface
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.4
python-multipart>=0.0.18

# Authentication
PyJWT>=2.9.0
# Encrypts provider API keys at rest in user_data/ai_settings.json.
cryptography>=43.0.0
# Optional at-rest database encryption. Only used when DB_ENCRYPTION_KEY is
# set; harmless (and small) otherwise. See tools/encrypt_databases.py.
sqlcipher3-binary>=0.5.4
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 introspects bcrypt.__about__.__version__, removed in bcrypt 4.1+.
# Pin below 4.1 to keep the passlib bcrypt backend working.
bcrypt>=4.0.1,<4.1

# Rate limiting
slowapi>=0.1.9

# Utilities
python-dotenv>=1.0.1
tqdm>=4.66.0

# Testing
pytest>=8.0.0